#!/usr/bin/env bash
set -euo pipefail

if [ $# -eq 0 ]; then
	echo 'Usage: timer <duration>' >&2
	echo 'Duration examples: 30s, 10m, 1h' >&2
	exit 1
fi

sleep "$1"
printf '\a'
notify 'timer complete' "$1"
