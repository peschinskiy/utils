#!/usr/bin/env bash
set -euo pipefail

if [ $# -ne 1 ]; then
	echo 'mksh takes one argument' >&2
	exit 1
elif [ -e "$1" ]; then
	echo "$1 already exists" >&2
	exit 1
fi

printf '#!/usr/bin/env bash\nset -euo pipefail\n\n' > "$1"
chmod u+x "$1"
"$EDITOR" "$1"
